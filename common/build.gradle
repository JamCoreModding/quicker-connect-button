architectury {
    common(rootProject.enabled_platforms.split(","))
}

loom {
    accessWidenerPath = file("src/main/resources/quickerconnectbutton.accesswidener")
}

dependencies {
    modImplementation libs.fabric.loader
    modApi libs.architectury.common
}
